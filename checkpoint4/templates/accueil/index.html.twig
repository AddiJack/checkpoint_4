{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="row">
        <div class="col-md-3">
            <a class="d-flex justify-content-center" href= "{{ path ('performance') }}">
                <img class="img_index" src="{{ asset('images/performance.png') }}"
                     alt="performance">
            </a>
        </div>
        <div class="col-md-3">
            <a class="d-flex justify-content-center" href= "{{ path ('about') }}">
                <img class="img_index" src="{{ asset('images/propos.png') }}"
                     alt="propos">
            </a>
        </div>
        <div class="col-md-3">
            <a class="d-flex justify-content-center" href= "{{ path ('price') }}">
                <img class="img_index" src="{{ asset('images/price.png') }}"
                     alt="price">
            </a>
        </div>
        <div class="col-md-3">
            <a class="d-flex justify-content-center" href= "{{ path ('contact') }}">
                <img class="img_index" src="{{ asset('images/contact.png') }}"
                     alt="contact">
            </a>
        </div>
    </div>
</div>
{% endblock %}
